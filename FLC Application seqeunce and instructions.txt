For the sequence,
First the weigh bridge user inputs the data of vehicle. The vehicle number field must be limited to 10 Characters and locked as ALPHANUMERIC only. The party/supplier can be selected from a list or search box/drop down. Then the gross weight would be picked from the weighbridge already installed in there. Then after receiving input of gross weight the QR/Random Serial Number would be generated and a print slip pop-up would come up. The print slip should not contain information for party/supplier name.
Follow by that, The Manager/Asst. Manager users can scan the QR using the phone camera or enter the serial number to access (generated by the weighbridge user) and further input only 3 fields. Mainly FLC(Fine Leaf Count %), Deduction will be deducted from net weight for Water/External Moisture % and Damage Leaf %. After the data is input by the second user/users (The Manager/Asst. Manager). The slip(QR) is then scanned or serial number is entered to access the data and the Net Weight for the vehicle is received from the weight bridge and the total weight challan informtion is full. The challan is then saved and a print pop-up opens. The print setup would be like 1/3 area per A4 size paper in Portrait. So that I can print three vehicle details in a A4 paper.
Apart from these two users there would be One Master User/Administrator who can setup users for weightbridge and Manager/Assistant manager with User ID and a Password for them to acess. The master user can also add, delete, modify party/suppliers list. There will be one more user as "Viewer/Management" who can only view the datas and print, and also only "add" new party/supplier name.